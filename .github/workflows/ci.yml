name: CI

on:
  push:
    branches:
      - gh-actions
  pull_request:
    branches:
      - gh-actions
  workflow_dispatch:

jobs:
  java-test:
    uses: ./.github/workflows/java-test-reusable.yml

  docker-ci:
    needs: java-test
    uses: ./.github/workflows/docker-ci-reusable.yml